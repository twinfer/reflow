// Reflow in-process SDK ↔ engine session envelope.
//
// SCOPE: This file describes the engine-side replay envelope used by
// today's in-process Go SDK path (engine.invoker → pkg/sdk in the same
// reflowd binary). The wire-facing engine↔handler protocol is
// proto/protocolv1/protocol.proto (Kind-aware,
// supports remote handlers via gRPC and HTTP/2). When the
// `engine-dials-handler` path lands (plan commit 5d/5e), this envelope
// is retained only for the in-proc dispatch path; the wire role flips
// to protocolv1.
//
//
// Both directions flow over the same transport. SDKMessage is a tagged
// envelope; the kinds enumerate the legal payloads in either direction.
// Direction conventions are documented per-message, not enforced by the
// schema — the engine and SDK each reject out-of-direction messages.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: sdkv1/sdk.proto

package sdkv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SessionService_Invoke_FullMethodName = "/reflow.sdk.v1.SessionService/Invoke"
)

// SessionServiceClient is the client API for SessionService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SessionService is the gRPC bidi-streaming entrypoint for out-of-process
// SDKs (TypeScript, Python, etc.). A handler-side SDK dials Invoke and the
// engine routes pending StartInvocation messages to it; the SDK responds
// with ProposeEntry / Completion / EndInvocation frames over the same
// stream.
//
// We deliberately use gRPC bidi-streaming here rather than Restate's
// raw-HTTP/2 length-prefix framing: it costs us nothing (gRPC is already
// in the dep tree for ingress), generated client stubs land for free in
// every language buf supports, and the wire format is well-tested.
//
// Phase 2 ships the proto definition and registers an Unimplemented
// server stub so clients can discover the service; routing to a connected
// SDK lands once a non-Go SDK actually exists. See SAD §11.
type SessionServiceClient interface {
	Invoke(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[SDKMessage, SDKMessage], error)
}

type sessionServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewSessionServiceClient(cc grpc.ClientConnInterface) SessionServiceClient {
	return &sessionServiceClient{cc}
}

func (c *sessionServiceClient) Invoke(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[SDKMessage, SDKMessage], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &SessionService_ServiceDesc.Streams[0], SessionService_Invoke_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[SDKMessage, SDKMessage]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SessionService_InvokeClient = grpc.BidiStreamingClient[SDKMessage, SDKMessage]

// SessionServiceServer is the server API for SessionService service.
// All implementations must embed UnimplementedSessionServiceServer
// for forward compatibility.
//
// SessionService is the gRPC bidi-streaming entrypoint for out-of-process
// SDKs (TypeScript, Python, etc.). A handler-side SDK dials Invoke and the
// engine routes pending StartInvocation messages to it; the SDK responds
// with ProposeEntry / Completion / EndInvocation frames over the same
// stream.
//
// We deliberately use gRPC bidi-streaming here rather than Restate's
// raw-HTTP/2 length-prefix framing: it costs us nothing (gRPC is already
// in the dep tree for ingress), generated client stubs land for free in
// every language buf supports, and the wire format is well-tested.
//
// Phase 2 ships the proto definition and registers an Unimplemented
// server stub so clients can discover the service; routing to a connected
// SDK lands once a non-Go SDK actually exists. See SAD §11.
type SessionServiceServer interface {
	Invoke(grpc.BidiStreamingServer[SDKMessage, SDKMessage]) error
	mustEmbedUnimplementedSessionServiceServer()
}

// UnimplementedSessionServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSessionServiceServer struct{}

func (UnimplementedSessionServiceServer) Invoke(grpc.BidiStreamingServer[SDKMessage, SDKMessage]) error {
	return status.Error(codes.Unimplemented, "method Invoke not implemented")
}
func (UnimplementedSessionServiceServer) mustEmbedUnimplementedSessionServiceServer() {}
func (UnimplementedSessionServiceServer) testEmbeddedByValue()                        {}

// UnsafeSessionServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SessionServiceServer will
// result in compilation errors.
type UnsafeSessionServiceServer interface {
	mustEmbedUnimplementedSessionServiceServer()
}

func RegisterSessionServiceServer(s grpc.ServiceRegistrar, srv SessionServiceServer) {
	// If the following call panics, it indicates UnimplementedSessionServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SessionService_ServiceDesc, srv)
}

func _SessionService_Invoke_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(SessionServiceServer).Invoke(&grpc.GenericServerStream[SDKMessage, SDKMessage]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SessionService_InvokeServer = grpc.BidiStreamingServer[SDKMessage, SDKMessage]

// SessionService_ServiceDesc is the grpc.ServiceDesc for SessionService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SessionService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "reflow.sdk.v1.SessionService",
	HandlerType: (*SessionServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Invoke",
			Handler:       _SessionService_Invoke_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "sdkv1/sdk.proto",
}
